export enum MessagePostProduct {
  createProduct = 'Producto creado exitosamente.',
}

export enum MessagePutProduct {
  updateProduct = 'Producto actualizado exitosamente.',
}

export enum MessageDeleteProduct {
  deleteProduct = 'Producto eliminado exitosamente.',
}

export enum MessageGetProduct {
  getProduct = 'Producto obtenido exitosamente.',
  productNotFound = 'Producto no encontrado.',
}
